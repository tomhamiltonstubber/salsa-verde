{% extends 'auth.jinja' %}

{% block content %}
  <div class="row">
    <form method="post">
      {% csrf_token %}
      <div class="edit-form">
        {{ form|bootstrap }}
        <button class="btn btn-primary" type="submit">Save</button>
      </div>
    </form>
  </div>
{% endblock %}
